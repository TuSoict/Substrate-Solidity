import { Header,Footer } from "../common";

export default function MainLayout({children}){
    return(
        <>
            <h1>Empty Layout</h1>
            {children}
        </>
    )
}